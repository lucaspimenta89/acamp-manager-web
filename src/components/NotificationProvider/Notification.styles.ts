import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
  root: {
    lineHeight: 1,
    padding: '30px 25px',
    '&&&': {
      fontWeight: 500,
      fontSize: '18px'
    }
  }
})